#include <stdio.h>
#include <math.h>
void main()
{
int a,b,c,d;
float r1,r2;
printf("enter a,b,c");
scanf("%d,%d,%d", &a,&b,&c);
if(d>0)
printf("r1=%f,r2=5f",r1,r2);
else if(d<0)
{
    printf("roots are imaginary");
    {
        
    {
        printf("roots are real and equal");
        
    }
    }
}
}
